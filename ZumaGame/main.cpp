#include <iostream>
#include <string>
#include <vector>

using namespace std;

int findMinStep(string board, string hand);

int main() {
	string board, hand;
	cin >> board >> hand;
	cout << findMinStep(board, hand) << endl;
	return 0;
}

int findMinStep(string board, string hand) {

}