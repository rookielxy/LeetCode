#include <iostream>
#include <vector>
#include <string>

using namespace std;

struct TreeNode {
	int val;
	TreeNode *left, *right;
	explicit TreeNode(int x): val(x), left(nullptr), right(nullptr) {}
};

int main() {

	return 0;
}

TreeNode* lcaDeepestLeaves(TreeNode* root) {

}